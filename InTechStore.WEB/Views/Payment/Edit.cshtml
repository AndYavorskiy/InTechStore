@model InTechStore.WEB.ViewModels.PaymentViewModel
@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm("Edit", "Payment", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()

    @Html.EditorForModel();
    @Html.ValidationSummary();
    <input type="submit" value="Підтвердити" />
}